# Keycloak end-user credentials used by app.py to fetch a JWT via the
# OAuth2 Resource Owner Password Credentials grant. These must match the
# user provisioned in keycloak-config/realm.json — change them in both
# places if you edit either. Not safe outside a local demo.
KEYCLOAK_USERNAME=alice@example.com
KEYCLOAK_PASSWORD=demo
